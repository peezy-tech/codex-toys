{
	"name": "@codex-toys/actions",
	"version": "0.140.6",
	"private": true,
	"description": "GitHub Actions auth and state helpers for Codex workbench runs.",
	"type": "module",
	"license": "Apache-2.0",
	"sideEffects": false,
	"types": "./dist/index.d.ts",
	"files": ["dist", "README.md"],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"engines": {
		"node": ">=24.0.0 <25"
	},
	"scripts": {
		"build": "vp run clean && tsc -p tsconfig.build.json",
		"check:types": "tsc --noEmit",
		"clean": "rm -rf dist",
		"prepack": "vp run build",
		"release:check": "vp run check:types && vp run build && vp run pack:dry-run",
		"pack:dry-run": "tsx ../../scripts/package-pack-dry-run.ts"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
