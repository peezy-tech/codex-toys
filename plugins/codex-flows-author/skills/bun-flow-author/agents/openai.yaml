interface:
  display_name: "Bun Flow Author"
  short_description: "Maintain custom or legacy Bun flow steps."
  default_prompt: "Review a custom Bun runner flow step only when the product owns that runner; otherwise recommend node flow steps or codex-flows turn automation."
