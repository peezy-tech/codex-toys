name: smoke

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  runner-smoke:
    runs-on: bun
    steps:
      - name: Check runner
        run: |
          bun --version
          echo "Forgejo runner is online"
