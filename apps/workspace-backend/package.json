{
	"name": "codex-workspace-backend-local",
	"version": "0.132.1",
	"description": "Local Codex workspace backend with app-server control and flow capabilities.",
	"type": "module",
	"private": true,
	"license": "Apache-2.0",
	"bin": {
		"codex-workspace-backend-local": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc --noEmit",
		"check:types": "tsc --noEmit",
		"start": "tsx ./src/index.ts serve",
		"test": "vp test run --root ../.. apps/workspace-backend/test"
	},
	"dependencies": {
		"@peezy.tech/codex-flows": "workspace:*",
		"ws": "catalog:"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@types/ws": "catalog:",
		"typescript": "catalog:"
	}
}
