{
	"name": "codex-app-cli",
	"version": "0.132.1",
	"description": "CLI for sending JSON-RPC commands to a running Codex app-server.",
	"type": "module",
	"private": true,
	"license": "Apache-2.0",
	"bin": {
		"codex-app": "./src/index.ts"
	},
	"scripts": {
		"build": "tsc --noEmit",
		"check:types": "tsc --noEmit",
		"test": "vp test run --root ../.. apps/cli/test"
	},
	"dependencies": {
		"@peezy.tech/codex-flows": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
